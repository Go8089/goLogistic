From e5374e6a2b2ac085b8c1f4a65d116ee8e8db628d Mon Sep 17 00:00:00 2001
From: go8089 <gopaljha8409@gmail.com>
Date: Tue, 1 Sep 2026 03:36:02 +0000
Subject: [PATCH 12/15] chore(db): add Flyway and migration for
 notification_channel; refactor admin to use PaymentService

---
 go_logistics_backend/pom.xml                  |  5 ++++
 .../com/goLogistic/admin/AdminController.java | 28 +++++++++----------
 .../V1__add_notification_channel_to_users.sql |  6 ++++
 3 files changed, 25 insertions(+), 14 deletions(-)
 create mode 100644 go_logistics_backend/src/main/resources/db/migration/V1__add_notification_channel_to_users.sql

diff --git a/go_logistics_backend/pom.xml b/go_logistics_backend/pom.xml
index a0a6a1f..8c07945 100644
--- a/go_logistics_backend/pom.xml
+++ b/go_logistics_backend/pom.xml
@@ -111,6 +111,11 @@
             <groupId>software.amazon.awssdk</groupId>
             <artifactId>elasticache</artifactId>
         </dependency>
+        <!-- Flyway for DB migrations -->
+        <dependency>
+            <groupId>org.flywaydb</groupId>
+            <artifactId>flyway-core</artifactId>
+        </dependency>
     </dependencies>
 
     <build>
diff --git a/go_logistics_backend/src/main/java/com/goLogistic/admin/AdminController.java b/go_logistics_backend/src/main/java/com/goLogistic/admin/AdminController.java
index a08a116..6288420 100644
--- a/go_logistics_backend/src/main/java/com/goLogistic/admin/AdminController.java
+++ b/go_logistics_backend/src/main/java/com/goLogistic/admin/AdminController.java
@@ -45,13 +45,16 @@ public class AdminController {
     private final PaymentRepository paymentRepository;
     private final VehicleRepository vehicleRepository;
 
+    private final com.goLogistic.payment.PaymentService paymentService;
+
     public AdminController(
         UserRepository userRepository,
         QuoteRequestRepository quoteRequestRepository,
         BookingRepository bookingRepository,
         ShipmentRepository shipmentRepository,
         PaymentRepository paymentRepository,
-        VehicleRepository vehicleRepository
+        VehicleRepository vehicleRepository,
+        com.goLogistic.payment.PaymentService paymentService
     ) {
         this.userRepository = userRepository;
         this.quoteRequestRepository = quoteRequestRepository;
@@ -59,6 +62,7 @@ public class AdminController {
         this.shipmentRepository = shipmentRepository;
         this.paymentRepository = paymentRepository;
         this.vehicleRepository = vehicleRepository;
+        this.paymentService = paymentService;
     }
 
     @GetMapping("/me")
@@ -380,20 +384,16 @@ public class AdminController {
         @PathVariable String id,
         @RequestBody Map<String, Object> payload
     ) {
-        Payment payment = paymentRepository.findByPaymentCode(id).orElse(null);
-        if (payment == null) {
-            return ResponseEntity.notFound().build();
-        }
-
-        String statusValue = String.valueOf(payload.getOrDefault("status", payment.getStatus().name()));
-        String normalizedStatus = statusValue.toUpperCase(Locale.ROOT).replace("PAID", "SUCCESS");
-        payment.setStatus(PaymentStatus.valueOf(normalizedStatus));
-        if (payment.getStatus() == PaymentStatus.SUCCESS && payment.getBooking() != null) {
-            payment.getBooking().setStatus(BookingStatus.CONFIRMED);
-            bookingRepository.save(payment.getBooking());
+        String statusValue = String.valueOf(payload.getOrDefault("status", ""));
+        try {
+            com.goLogistic.payment.Payment updated = paymentService.updatePaymentStatus(id, statusValue);
+            return ResponseEntity.ok(toPaymentMap(updated));
+        } catch (IllegalArgumentException ex) {
+            return ResponseEntity.badRequest().body(Map.of("message", ex.getMessage()));
+        } catch (IllegalStateException ex) {
+            return ResponseEntity.status(409).body(Map.of("message", ex.getMessage()));
         }
-        Payment saved = paymentRepository.save(payment);
-        return ResponseEntity.ok(toPaymentMap(saved));
+    }
     }
 
     @PutMapping("/users/{id}/notification-channel")
diff --git a/go_logistics_backend/src/main/resources/db/migration/V1__add_notification_channel_to_users.sql b/go_logistics_backend/src/main/resources/db/migration/V1__add_notification_channel_to_users.sql
new file mode 100644
index 0000000..379139b
--- /dev/null
+++ b/go_logistics_backend/src/main/resources/db/migration/V1__add_notification_channel_to_users.sql
@@ -0,0 +1,6 @@
+-- Add preferred notification channel to users table with default EMAIL
+ALTER TABLE users
+ADD COLUMN IF NOT EXISTS notification_channel VARCHAR(32) NOT NULL DEFAULT 'EMAIL';
+
+-- Backfill existing NULLs just in case
+UPDATE users SET notification_channel = 'EMAIL' WHERE notification_channel IS NULL;
\ No newline at end of file
-- 
2.43.0

